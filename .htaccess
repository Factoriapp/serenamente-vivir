# Configuración de Caché para evitar problemas en desarrollo activo
# Desactiva caché para HTML/JSON/XML para ver cambios inmediatamente
<IfModule mod_expires.c>
    ExpiresActive On
    
    # HTML siempre fresco (0 segundos de caché)
    ExpiresByType text/html "access plus 0 seconds"
    
    # Datos (JSON/XML) siempre frescos
    ExpiresByType application/json "access plus 0 seconds"
    ExpiresByType text/xml "access plus 0 seconds"

    # Assets estáticos (CSS/JS/Imágenes) con caché moderado (1 semana)
    # Esto mantiene la velocidad, pero obliga a usar ?v=xx si cambiamos algo crítico
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
    ExpiresByType image/jpeg "access plus 1 week"
    ExpiresByType image/png "access plus 1 week"
    ExpiresByType image/gif "access plus 1 week"
    ExpiresByType image/svg+xml "access plus 1 week"
</IfModule>

# Seguridad básica: Evitar listar directorios
Options -Indexes
